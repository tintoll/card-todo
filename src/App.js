import React, { Component } from 'react';
import { Header, Layout } from "components";

class App extends Component {
  render() {
    return (
      <div>
        <Header />
        <Layout />
      </div>
    );
  }
}

export default App;
